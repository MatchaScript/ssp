---
'@matchalatte/ssp-ui': patch
---

In row mode collections, a bare `a` or `A` keypress now routes to typeahead search when select-all is not performed, matching cell mode behavior and allowing rows starting with "a" to be reached by typing.
